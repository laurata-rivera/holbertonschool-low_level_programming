#include "main.h"

/**
 * mul - multiplies two integers
 * @a: Input parameter integers
 * @b: Input parameter integers
 *
 * Return: On success 1
 * On error, -1 is returned, and errno is set appopiatly
 */

int mul(int a, int b)
{
	return (a * b);
}
